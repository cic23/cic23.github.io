# 지킴이 로그 반응 속도 개선 계획

작성일: 2026-09-13

## 목표

- 게시물 선택 시 이미 받은 목록 정보와 브라우저 캐시를 먼저 표시하고, 상세 요청은 필요한 경우에만 수행한다.
- 좋아요는 클릭 즉시 하트와 숫자를 바꾸고, 서버 응답으로 최종 상태를 확정한다.
- 댓글 등록 뒤 게시물 전체를 다시 불러오거나 다시 그리지 않고, 댓글 영역만 갱신한다.
- Apps Script 요청마다 동일한 Google Sheet를 반복해서 읽지 않는다.

## 구현 순서

1. 게시물 상세 응답을 브라우저 메모리에 보관한다. 같은 게시물·댓글 페이지를 다시 열면 캐시된 상세를 즉시 표시한다.
2. 좋아요 클릭 때 화면 상태를 즉시 반전하고 숫자를 증감한다. 요청 성공 시 서버의 `liked`와 `likeCount`로 정정하고, 실패 시 이전 상태로 복구한다.
3. 댓글 생성 API가 새 댓글의 페이지와 전체 댓글 수를 함께 반환한다. 현재 마지막 댓글 페이지라면 새 댓글 DOM과 댓글 수만 추가하고, 새 페이지가 필요할 때만 그 댓글 페이지로 이동한다.
4. `listPosts`와 `getPost`에서 Posts, Comments, Attachments, Likes를 각각 한 번씩 읽어 메모리 색인으로 공개 게시물 데이터를 만든다. 게시물·첨부·좋아요마다 Sheet를 다시 읽는 호출을 제거한다.
5. 회귀 테스트로 댓글 응답 메타데이터와 좋아요·댓글 UI의 즉시 반영을 확인하고, 기존 백엔드·전송 테스트 및 배포 전 문법 검사를 실행한다.

## 검증 기준

- 정상 댓글 등록은 `createComment` 한 번으로 끝나며, 게시물 상세 `getPost` 재요청이 발생하지 않는다.
- 좋아요 클릭 직후 숫자와 하트 상태가 보이고, 실패하면 정확히 원복된다.
- 목록과 상세 조회는 각 테이블을 요청당 한 번만 읽는다.
- 31번째 댓글처럼 새 페이지가 필요한 경우에도 중복 댓글 없이 마지막 페이지로 이동한다.
